export default function randomUnique() {
  /* const choices = [];
  for(let i=0; i<3; i++){
    choices.push(Math.floor(Math.random() * 730) +1)
  }
  const isAllDiff = choices.every((e,i) =>{
    return e!== choices[i+1]
  });

  if(isAllDiff) return choices;
  else{
    return randomUnique();
  } */
  //69 310 620 485 498
  return [70, 310, 620];
}
